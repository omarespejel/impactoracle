# ===========================================
# REQUIRED
# ===========================================

# Your wallet address to receive payments (use a fresh one for testnet)
PAY_TO_ADDRESS=0xYourWalletAddressHere

# EigenAI API key (get from https://app.eigenai.com)
EIGENAI_API_KEY=your_eigenai_api_key

# Base Sepolia RPC (get free from Alchemy or QuickNode)
BASE_SEPOLIA_RPC_URL=https://base-sepolia.g.alchemy.com/v2/your_key

# ===========================================
# OPTIONAL (with defaults)
# ===========================================

# EigenAI endpoint (correct URL based on docs)
EIGENAI_BASE_URL=https://app.eigenai.com/api/v1

# x402 Facilitator (Coinbase hosted - handles payment verification)
X402_FACILITATOR_URL=https://x402.coinbase.com/facilitator

# Price per verification in cents (5 = $0.05)
PRICE_CENTS=5

# Environment
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# ===========================================
# PRODUCTION ONLY
# ===========================================

# Redis URL for rate limiting (optional, uses memory store if not set)
# REDIS_URL=redis://localhost:6379
